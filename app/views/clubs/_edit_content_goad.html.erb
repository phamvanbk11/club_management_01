<div class="col-md-12 col-md-offset-2">
  <div class="modal-dialog edit-club modal-lg">
    <div class="modal-content">
      <div class="modal-body">
        <div class="block">
          <div class="row edit-organization">
            <h2 class="text-center col-md-12"><%= @club.name %></h2>
            <div class="col-md-12 col-md-offset-1">
              <%= form_for [@club.organization, @club],
                html: {multipart: true}, remote: :true do |f| %>
                <%= hidden_field_tag :tab, :content %>
                <div class="album-errors"></div>
                <div class="form-group col-md-12">
                  <%= f.label(:content, class: "control-label") do %>
                    <%= t("content_club") %>
                  <% end %>
                  <%= f.cktext_area :content, class: "form-control input-text" %>
                </div>
                <div class="form-group col-md-12">
                  <%= f.label(:goal, class: "control-label") do %>
                    <%= t("goal") %>
                  <% end %>
                  <%= f.cktext_area :goal, class: "form-control input-text" %>
                </div>
                <div class="text-center col-md-12">
                  <%= f.submit t("update"), class: "btn btn-primary col-md-2 btn-sm" %>
                  <%= f.button t("close"), class: "btn btn-sm btn-warning",
                    "data-dismiss": "modal" %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
