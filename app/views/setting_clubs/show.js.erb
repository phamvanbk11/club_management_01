if ($('.js-check-modal').length === 0){
  $('#config-club').html('<%= j render "member_frequency" %>');
}
else {
  $('#js-list-users').html('<%= j render "list_users_body" %>');
  $('#js-header-users').html('<%= j render "header_modal" %>')
}
if (($("#config-club").data('bs.modal') || {})._isShown != true){
  $('#config-club').modal('show');
}
